# 🚀 СРОЧНО: Исправление загрузки изображений на проде

## Проблема

На Vercel нельзя записывать файлы в `/public/ProductImages` — файловая система read-only.

## Решение (5 минут)

### 1. Установи зависимость локально

```bash
npm install @vercel/blob
```

### 2. Закоммить и запушь

```bash
git add package.json package-lock.json src/app/api/admin/upload/route.ts
git commit -m "fix: использовать Vercel Blob для загрузки изображений"
git push
```

### 3. Настрой Vercel Blob

1. Открой https://vercel.com/dashboard
2. Выбери проект `poj-pro`
3. Вкладка **Storage** → **Create Database**
4. Выбери **Blob**
5. Имя: `poj-pro-blob` → **Create**

### 4. Проверь переменную окружения

После создания Blob:
1. Vercel Dashboard → poj-pro → Settings → Environment Variables
2. Должна быть `BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx...`
3. Если нет — добавь вручную (значение возьми из Vercel Storage)

### 5. Подожди деплой

Vercel автоматически задеплоит изменения через 1-2 минуты.

---

## ✅ Проверка

После деплоя:

1. Открой https://www.poj-pro.uz/admin-products-add
2. Введи токен администратора
3. Попробуй загрузить изображение
4. Должно работать! 🎉

---

## 📊 Тарифы

Бесплатно хватит:
- 1 GB хранилища
- 1 GB загрузок в месяц
- 10 GB трафика

Для магазина огнетушителей — более чем достаточно.

---

## 🆘 Если что-то пошло не так

### "Хранилище не настроено"

Добавь переменную в Vercel:
```
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx...
```

Где взять токен:
- Vercel Dashboard → Storage → Blob → poj-pro-blob → Tokens

### Изображения не отображаются

Vercel Blob возвращает полные URL вида:
```
https://xxxxx.public.blob.vercel-storage.com/ProductImages/file.webp
```

Это нормально. Они будут работать.

---

## 📝 Что изменилось

**До:**
```ts
// Запись в файловую систему
await writeFile('/public/ProductImages/file.webp', buffer)
```

**После:**
```ts
// Запись в Vercel Blob
await put('ProductImages/file.webp', file)
// Возвращает URL: https://xxx.public.blob.vercel-storage.com/ProductImages/file.webp
```

---

## 🔗 Документация

- Полная инструкция: `docs/VERCEL_BLOB_SETUP.md`
- Vercel Blob Docs: https://vercel.com/docs/storage/vercel-blob
