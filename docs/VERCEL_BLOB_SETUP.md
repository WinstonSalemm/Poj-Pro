# Настройка загрузки изображений на Vercel

## ⚠️ Проблема

При деплое на Vercel файловая система становится **read-only**. Нельзя записывать файлы в `/public/ProductImages` — они исчезнут после перезапуска сервера.

## ✅ Решение: Vercel Blob Storage

Vercel предоставляет собственное S3-совместимое хранилище для файлов.

---

## 📋 Инструкция по настройке

### Шаг 1: Установи зависимость

```bash
npm install @vercel/blob
```

### Шаг 2: Создай Blob Storage в Vercel

1. Зайди в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выбери свой проект `poj-pro`
3. Перейди во вкладку **Storage**
4. Нажми **Create Database**
5. Выбери **Blob**
6. Дай имя (например `poj-pro-blob`)
7. Нажми **Create**

### Шаг 3: Добавь переменную окружения

После создания Blob Storage:

1. Vercel автоматически создаст переменную `BLOB_READ_WRITE_TOKEN`
2. Скопируй её значение
3. Добавь в настройки проекта:
   - Vercel Dashboard → Project Settings → Environment Variables
   - Добавь `BLOB_READ_WRITE_TOKEN` со значением
   - Добавь для всех окружений (Production, Preview, Development)

Или через CLI:
```bash
vercel env add BLOB_READ_WRITE_TOKEN vercel_blob_rw_xxx...
```

### Шаг 4: Обнови .env локально

```env
# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx...
```

### Шаг 5: Задеплой изменения

```bash
git add .
git commit -m "fix: использовать Vercel Blob для загрузки изображений"
git push
```

Vercel автоматически задеплоит изменения.

---

## 🔍 Как это работает

### До изменений:
```
Загрузка → /public/ProductImages/file.webp → Ошибка на Vercel (read-only FS)
```

### После изменений:
```
Загрузка → Vercel Blob Storage → URL: https://xxx.public.blob.vercel-storage.com/ProductImages/file.webp
```

Изображения хранятся в облаке, URL сохраняется в базу данных.

---

## 📊 Тарифы Vercel Blob

Бесплатный план (Hobby):
- 1 GB хранилища
- 1 GB загрузка в месяц
- 10 GB загрузка (egress) в месяц

Платные планы от $10/мес:
- Больше хранилища
- Больше трафика

Для небольшого магазина хватит бесплатного плана.

---

## 🛠 Альтернативы

Если не хочешь использовать Vercel Blob:

### 1. Cloudflare R2 (дешевле)
- 10 GB бесплатно
- S3-совместимый API
- Настройка через `@aws-sdk/client-s3`

### 2. AWS S3
- Классическое решение
- Платишь за что используешь

### 3. Self-hosted на VPS
- Если переедешь на свой сервер (DigitalOcean, Hetzner, etc.)
- Тогда можно будет писать в файловую систему
- Верни старую версию `upload/route.ts` из git

---

## 🧪 Тестирование

После деплоя:

1. Открой https://www.poj-pro.uz/admin-products-add
2. Введи токен администратора
3. Попробуй загрузить изображение
4. Проверь что:
   - Изображение загрузилось
   - Превью отображается
   - Товар сохраняется с изображением

---

## 🐛 Отладка

### Ошибка: "Хранилище не настроено"

**Проблема:** Нет переменной `BLOB_READ_WRITE_TOKEN`

**Решение:**
```bash
# Проверь локально
echo $BLOB_READ_WRITE_TOKEN

# Добавь в Vercel Dashboard
# Project Settings → Environment Variables → Add
```

### Ошибка: "Unauthorized"

**Проблема:** Неверный токен администратора

**Решение:** Проверь что вводишь правильный пароль (по умолчанию `admin-ship-2025`)

### Ошибка: "Failed to upload to Vercel Blob"

**Проблема:** Возможно закончилось место или проблемы с токеном

**Решение:**
1. Проверь Vercel Dashboard → Storage → Blob → Usage
2. Пересоздай токен если нужно

---

## 📁 Структура хранения

```
Vercel Blob Storage:
└── ProductImages/
    ├── 1771495108562-5e5aa299-8e6d-474f-9824-244755e427ae.webp
    ├── 1771495200123-abc123def-4567-8901-ghijklmnopqr.png
    └── ...
```

Все изображения публичные (`access: 'public'`), доступны по прямому URL.

---

## 📝 Миграция старых изображений

Старые изображения остались в `/public/ProductImages/` и продолжат работать.

Новые изображения будут загружаться в Vercel Blob.

Если хочешь перенести старые изображения в Blob:
1. Скачай их из git
2. Загрузи через админку заново
3. Обнови товары с новыми URL

Или напиши скрипт для автоматической загрузки.

---

## 🔗 Полезные ссылки

- [Vercel Blob Documentation](https://vercel.com/docs/storage/vercel-blob)
- [Vercel Blob SDK](https://github.com/vercel/storage/tree/main/packages/blob)
- [Vercel Pricing](https://vercel.com/pricing)
