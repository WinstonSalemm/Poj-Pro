# Загрузка изображений — документация

## 📦 Хранение изображений

### Локальная разработка
Изображения сохраняются в `/public/ProductImages/`

### Продакшен (Vercel)
Изображения сохраняются в **Vercel Blob Storage** (облачное хранилище)

**Почему:** Vercel не позволяет записывать файлы в файловую систему — они исчезнут после перезапуска сервера.

---

## 🔧 Настройка для продакшена

### 1. Установи зависимость

```bash
npm install @vercel/blob
```

### 2. Создай Blob Storage в Vercel

1. Vercel Dashboard → Твой проект → Storage
2. Create Database → Blob
3. Дай имя, нажми Create

### 3. Добавь переменную окружения

Vercel автоматически создаст `BLOB_READ_WRITE_TOKEN`. Проверь что она есть:
- Vercel Dashboard → Project Settings → Environment Variables
- Должна быть `BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx...`

### 4. Задеплой

```bash
git push
```

---

## 📋 Полная документация

Смотри `docs/VERCEL_BLOB_SETUP.md` — подробная инструкция по настройке.

---

## 🧪 Тестирование

### Локально

```bash
npm run dev
```

Открой: http://localhost:3000/admin-products-add

### На проде

Открой: https://www.poj-pro.uz/admin-products-add

---

## 🐛 Отладка

### Ошибка: "Хранилище не настроено"

**Проблема:** Нет переменной `BLOB_READ_WRITE_TOKEN`

**Решение:** Добавь в Vercel Dashboard → Project Settings → Environment Variables

### Ошибка: "Unauthorized"

**Проблема:** Неверный токен администратора

**Решение:** Проверь пароль (по умолчанию `admin-ship-2025`)

---

## 📁 Логи

### Консоль браузера (F12)
- `[Frontend] Starting upload for files:`
- `[Frontend] Upload response:`
- `[Preview Error] Failed to load image:`

### Консоль сервера (Vercel Logs)
- `[upload] Processing file:`
- `[upload] Uploading to Vercel Blob:`
- `[upload] Upload successful:`
